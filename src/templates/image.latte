{if $caption}
    <figure>
        {if $lightbox}
            <a href="{$linkSrcSet}">
                {$imgTag|noescape}
            </a>
        {else}
            {$imgTag|noescape}
        {/if}

        <figcaption>{$caption}</figcaption>
    </figure>
{else}
    {$imgTag|noescape}
{/if}